#logging.level.org.springframework.web=INFO
spring.thymeleaf.cache=false

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/Library?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false&allowPublicKeyRetrieval=true
spring.datasource.username = root
spring.datasource.password = nR7w1Q5c
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update


#server.servlet.context-path=/springboot-crud-rest
#server.session.tracking-modes=cookie

security.basic.enable: false
security.ignored=

bezkoder.app.jwtSecret= toto
bezkoder.app.jwtExpirationMs= 86400000
